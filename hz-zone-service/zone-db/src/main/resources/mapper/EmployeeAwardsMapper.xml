<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zcxd.db.mapper.EmployeeAwardsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zcxd.db.model.EmployeeAwards">
        <id column="id" property="id" />
        <result column="emp_id" property="empId" />
        <result column="awards_type" property="awardsType" />
        <result column="awards_date" property="awardsDate" />
        <result column="comments" property="comments" />
        <result column="create_user" property="createUser" />
        <result column="create_time" property="createTime" />
        <result column="update_user" property="updateUser" />
        <result column="update_time" property="updateTime" />
        <result column="deleted" property="deleted" />
    </resultMap>
    
    <select id="findListByPage" resultType="java.util.Map" >
		SELECT
			ea.id,
			ea.emp_id AS empId,
			e.emp_no AS empNo,
			e.emp_name AS empName,
			ea.awards_type AS awardsType,
			ea.comments,
			ea.awards_date AS awardsDate
		FROM
			employee_awards ea
		INNER JOIN employee e ON ea.emp_id = e.id
		<where>
			<if test="empNo != null and empNo != ''">
				and e.emp_no=#{empNo}
			</if>
			<if test="empName != null and empName != ''">
				and e.emp_name like '%${empName}%'
			</if>
			<if test="awardsType != null and awardsType != ''">
				and awards_type = #{awardsType}
			</if>
			<if test="deleted != null">
				and ea.deleted = #{deleted}
			</if>
		</where>
		order by ea.create_time desc
	</select>

</mapper>
